[tool.uv.workspace]
members = [
    "apps/starrail",
    "apps/wuwa",
    "packages/game-automation-core",
]

[tool.pytest.ini_options]
addopts = ["--import-mode=importlib"]
testpaths = [
    "packages/game-automation-core/tests",
    "apps/starrail/tests",
    "apps/wuwa/tests",
]
